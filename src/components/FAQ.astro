---
interface Props { items: { q: string; a: string }[]; title?: string }
const { items, title = 'Lo que las familias nos preguntan primero' } = Astro.props;
const schema = { '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: items.map(i => ({ '@type': 'Question', name: i.q, acceptedAnswer: { '@type': 'Answer', text: i.a } })) };
---
<section class="faq-section">
  <div class="wrap">
    <p class="eyebrow">Respuestas honestas</p>
    <h2>{title}</h2>
    <div class="faq">
      {items.map(i => (
        <details>
          <summary>{i.q}</summary>
          <p set:html={i.a} />
        </details>
      ))}
    </div>
  </div>
</section>
<script type="application/ld+json" set:html={JSON.stringify(schema)} />
